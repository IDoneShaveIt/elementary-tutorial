version: 2

models:
  - name: stg_customers
    config:
      tags: ["staging", "PII"]
    columns:
      - name: customer_id

  - name: stg_orders
    config:
      tags: ["staging", "finance"]
    columns:
      - name: order_id
      - name: status

      - name: ORDER_ID
        tests:
          - unique:
              config:
                tags:
                  - finance
                severity: error
              meta:
                owner:
                  - maayan+1337@elementary-data.com
                description: The column should contain only unique values
  - name: stg_payments
    config:
      tags: ["staging", "finance"]
    columns:
      - name: payment_id
      - name: payment_method

      - name: ORDER_ID
        tests:
          - unique:
              config:
                tags:
                  - finance
                severity: error
              meta:
                owner:
                  - maayan+1337@elementary-data.com
                description: The column should contain only unique values
  - name: stg_signups
    config:
      tags: ["staging", "PII"]
    columns:
      - name: signup_id
      - name: customer_email
