version: 2
models:
  - name: lost_orders
    columns:
      - name: order_id
        tests:
          - unique:
              config:
                severity: error
              meta:
                description: The column should contain only unique values
      - name: customer_id
        tests:
          - unique:
              config:
                severity: error
              meta:
                description: The column should contain only unique values
  - name: order_items
    columns:
      - name: customer_id
        tests:
          - unique:
              config:
                severity: error
              meta:
                description: The column should contain only unique values
      - name: order_id
        tests:
          - unique:
              config:
                severity: error
              meta:
                description: The column should contain only unique values
