version: 2
models:
  - name: stats_team
    columns:
      - name: DRIBBLES
        tests:
          - accepted_values:
              values:
                - val1
                - val2
              quote: true
              config:
                severity: error
              meta:
                description: 'The DRIBBLES column in the stats_team table should be one of the following values: val1, val2'
