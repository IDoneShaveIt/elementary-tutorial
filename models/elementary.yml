version: 2
models:
  - name: lost_orders
    columns:
      - name: order_id
        tests:
          - unique:
              config:
                tags:
                  - finance
                severity: error
              meta:
                owner:
                  - Idan
                description: The column should contain only unique values
      - name: customer_id
        tests:
          - unique:
              config:
                tags:
                  - finance
                severity: error
              meta:
                owner:
                  - Idan
                description: The column should contain only unique values
