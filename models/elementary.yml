version: 2
models:
  - name: string_column_anomalies
    columns:
      - name: AVERAGE_LENGTH
        tests:
          - unique:
              config:
                severity: error
              meta:
                description: The column should contain only unique values
