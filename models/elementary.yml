version: 2
models:
  - name: stats_team
    columns:
      - name: PENALTIES_NOT_SCORED
        tests:
          - not_null:
              config:
                severity: error
              meta:
                description: The PENALTIES_NOT_SCORED column in the stats_team table should not contain null values dsfsdf
