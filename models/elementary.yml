version: 2
models:
  - name: stats_team
    tests:
      - elementary.freshness_anomalies:
          time_bucket:
            period: day
            count: 1
          sensitivity: 3
          days_back: 14
          backfill_days: 2
          where_expression: my where clause
          timestamp_column: mucol
          config:
            severity: error
          meta:
            owner:
              - nimrod+333@shaked.dev
            description: Checking for freshness anomalies by looking at a 1 days period and comparing it to last 14 days
