version: 2
models:
  - name: stats_team
    columns:
      - name: TOTAL_SHOTS
        tests:
          - relationships:
              to: ref('col2')
              field: col1
              config:
                error_if: <3
                warn_if: '>22'
              meta:
                description: Each existing TOTAL_SHOTS column in the stats_team table should have a correlating col1 in the col2 table
