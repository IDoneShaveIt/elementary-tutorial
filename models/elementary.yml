version: 2
models:
  - name: order_items
    columns:
      - name: customer_id
        tests:
          - not_null:
              config:
                severity: error
              meta:
                owner:
                  - Maayan
                description: The column should not contain null values
      - name: order_id
        tests:
          - not_null:
              config:
                severity: error
              meta:
                owner:
                  - Maayan
                description: The column should not contain null values
  - name: lost_orders
    columns:
      - name: order_id
        tests:
          - not_null:
              config:
                severity: error
              meta:
                owner:
                  - Maayan
                description: The column should not contain null values
      - name: customer_id
        tests:
          - not_null:
              config:
                severity: error
              meta:
                owner:
                  - Maayan
                description: The column should not contain null values
