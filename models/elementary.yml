version: 2
models:
  - name: one
    tests:
      - elementary.freshness_anomalies:
          time_bucket:
            period: day
            count: 1
          sensitivity: 3
          days_back: 14
          backfill_days: 2
          seasonality: day_of_week
          timestamp_column: col1
          config:
            severity: error
          meta:
            description: Checking for freshness anomalies by looking at a 1 days period and comparing it to last 14 days
