version: 2
models:
  - name: stats_team
    columns:
      - name: DRIBBLES
        tests:
          - relationships:
              to: ref('mytable')
              field: mycol
              config:
                severity: error
              meta:
                description: Each existing DRIBBLES column in the stats_team table should have a correlating mycol in the mytable table
