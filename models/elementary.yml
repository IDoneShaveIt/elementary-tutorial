version: 2
models:
  - name: stats_team
    columns:
      - name: FOULS_CONCEDED
        tests:
          - accepted_values:
              values:
                - val1
              config:
                severity: error
              meta:
                description: 'The FOULS_CONCEDED column in the stats_team table should be one of the following values: val1'
