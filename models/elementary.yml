version: 2
models:
  - name: stats_team
    columns:
      - name: DRIBBLES
        tests:
          - unique:
              config:
                severity: error
              meta:
                description: The DRIBBLES column in the stats_team table should contain only unique values
