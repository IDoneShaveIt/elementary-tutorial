version: 2
models:
  - name: stats_team
    columns:
      - name: TEAM
        tests:
          - unique:
              config:
                severity: error
              meta:
                description: The column should contain only unique values
      - name: RED_CARDS
        tests:
          - unique:
              config:
                severity: error
              meta:
                description: The column should contain only unique values
      - name: DRIBBLES
        tests:
          - unique:
              config:
                severity: error
              meta:
                description: The column should contain only unique values
