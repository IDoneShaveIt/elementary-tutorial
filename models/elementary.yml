version: 2
models:
  - name: stats_team
    columns:
      - name: SUBS_ON
        tests:
          - unique:
              config:
                severity: error
              meta:
                description: The SUBS_ON column in the stats_team table should contain only unique values
